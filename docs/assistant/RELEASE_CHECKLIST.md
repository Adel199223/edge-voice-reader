# Release Checklist

- keep the product identity as a lightweight Edge extension unless the user explicitly changes scope
- rerun the harness validation commands after harness-doc changes
- rerun the extension static checks after JS or manifest changes
- rerun the Windows Edge smoke check after playback or selection-path changes
- confirm the saved voice and speed still persist across popup reopen and browser restart
- keep Ava and Andrew as the only surfaced voices unless the roadmap changes deliberately
